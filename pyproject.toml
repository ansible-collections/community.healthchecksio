[project]
name = "community-healthchecksio"
version = "0.0.0"
description = "Ansible collection for Healthchecks.io (see galaxy.yml for collection version)"
readme = "README.md"
requires-python = ">=3.10"

[dependency-groups]
dev = [
    "antsibull-changelog>=0.25",
]

[tool.uv]
package = false
